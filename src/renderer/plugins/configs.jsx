// Export all the configs here for the Settings plugin to use,
// except Settings itself, which is imported separately
const pluginConfigs = [
    require('./highscores/config.json')
];

export default pluginConfigs;
