{
  "name": "Settings",
  "author": "allie",
  "version": "0.1.0",
  "description": "Core settings for Eterlite.",
  "scope": "eterlite",
  "settings": {
    "clientSize": {
      "label": "Client size",
      "description": "Set the exact size of the game client area.",
      "type": "multiInput",
      "inputs": {
        "width": {
          "label": "Width",
          "type": "number",
          "min": 900,
          "default": 900
        },
        "height": {
          "label": "Height",
          "type": "number",
          "min": 600,
          "default": 600
        }
      }
    },
    "rememberWindow": {
      "label": "Save window size and position",
      "description": "If enabled, the window will restore its previous size and position when launching.",
      "type": "toggle",
      "default": true
    },
    "loadingScreenSounds": {
      "label": "Loading screen sound effects",
      "description": "Enables or disables sound effects on the client loading screen.",
      "type": "toggle",
      "default": true
    },
    "enabledPlugins": {
      "default": []
    },
    "enabledTools": {
      "default": []
    },
    "windowBounds": {
      "x": {
        "default": 0
      },
      "y": {
        "default": 0
      },
      "width": {
        "default": 933
      },
      "height": {
        "default": 681
      }
    }
  }
}
